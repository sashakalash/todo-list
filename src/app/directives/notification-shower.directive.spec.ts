import { NotificationShowerDirective } from './notification-shower.directive';

describe('NotificationShowerDirective', () => {
  it('should create an instance', () => {
    const directive = new NotificationShowerDirective();
    expect(directive).toBeTruthy();
  });
});
